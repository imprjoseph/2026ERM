import type { MetadataRoute } from "next";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.PUBLIC_SITE_URL||"https://forum.example.org";return ["","/2026","/2026/speakers","/2026/agenda","/dialogues","/register","/privacy","/personal-data","/terms"].map((path)=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly" as const,priority:path===""?1:0.7}))}
